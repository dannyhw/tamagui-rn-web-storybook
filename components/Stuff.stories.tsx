import { Meta } from "@storybook/react";

import { Stuff } from "./Stuff";

export default {
  title: "components/Stuff",
  component: Stuff,
} as Meta<typeof Stuff>;

export const Basic = {
  args: {},
};
