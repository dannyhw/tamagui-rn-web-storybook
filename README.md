Tamagui running in react native web storybook with vite

important things

- vite.config.js this gets loaded automatically by storybook
- tamagui.config.js
- preview.tsx we add a decorator to wrap the storybook with the tamagui provider
